{
  "name": "kudu-client-redux-actions",
  "version": "0.1.0",
  "main": "lib/actions.js",
  "scripts": {
    "compile": "babel -d lib/ src/",
    "lint": "eslint {src,test}/*.js; true",
    "test": "mocha test/ -R spec --recursive --compilers js:babel/register",
    "prepublish": "npm run compile"
  },
  "peerDependencies": {
    "kudu-client": "^0.5.0",
    "redux": "^3.0.0",
    "redux-thunk": "^1.0.0"
  },
  "devDependencies": {
    "babel": "^5.8.34",
    "babel-eslint": "^4.1.5",
    "chai": "^3.4.1",
    "eslint": "^1.9.0",
    "mocha": "^2.3.4",
    "nock": "^3.1.0"
  }
}
